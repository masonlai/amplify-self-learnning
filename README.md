Read the Identity Verification wireframe (BPIC Account Opening MVP1
Wireframes v13.pdf, step 7 of 15) and the existing ClientProfile TS types
plus the profile save/update API contract in this repo.

Produce a field list for the Identity Verification screen: for each field
give the UI label, a proposed API field name following this repo's existing
naming convention, the type, whether it's required, and which section and
verification method it belongs to.

Flag anything where the wireframe label is ambiguous or where a similar
field already exists in ClientProfile under a different name.
